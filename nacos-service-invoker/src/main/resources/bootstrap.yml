spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # 配置地址 nacos服务器地址
      config:
        group: DEFAULT_GROUP #指定 ACM 配置的 Group，默认为 DEFAULT_GROUP
        file-extension: properties #指定配置的 Data ID 后缀，支持 properties、yaml、yml，默认为 properties
        server-addr: 127.0.0.1:8848 # 配置地址 nacos服务器地址